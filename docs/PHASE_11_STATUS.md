# Phase 11 status

Implementation is complete on `phase-11-performance-calibration` and ready for pull-request CI.

The merge gate is the repository CI suite plus the new v0.8 performance-calibration regressions. If any job fails, the branch must be corrected before merge.
