# v1.2.5 technical source-lock source pack

The full `technical_source_lock_audit_1980.csv` contains 285 row-level audit records (~199 KB) and remains part of the external canonical database bundle. Its exact SHA-256 is pinned in `../CHECKSUMS.json`.

This repository keeps the compact policy/readiness/manifest/diff/unknown/suppression outputs required to explain and validate the release without duplicating every external editorial artifact.
